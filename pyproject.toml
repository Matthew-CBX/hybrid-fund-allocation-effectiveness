[project]
name = "hybrid-fund-allocation-effectiveness"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "numpy>=1.26,<3",
  "pandas>=2.0,<3",
]

[tool.pytest.ini_options]
pythonpath = ["src", "."]
testpaths = ["tests"]
